import { Theme, SerializedStyles } from "@emotion/react";
import { Size } from "./Avatar";
export declare const avatar: ({ typeScaleSizes }: Theme, { size, bgColor }: {
    size: Size;
    bgColor: string;
}) => SerializedStyles;
