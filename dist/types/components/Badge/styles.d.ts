import { Theme, SerializedStyles } from "@emotion/react";
import { Offset, Size } from "./Badge";
export declare const container: ({ badge }: Theme, { size, offset }: {
    size: Size;
    offset: Offset;
}) => SerializedStyles;
