import { Theme, SerializedStyles } from "@emotion/react";
import { InputSize } from "./Input";
export declare const inputContainer: ({ formElements }: Theme, { size }: {
    size: InputSize;
}) => SerializedStyles;
export declare const fileInputContainer: ({ formElements }: Theme) => SerializedStyles;
