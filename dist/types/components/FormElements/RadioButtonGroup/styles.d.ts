import { Theme, SerializedStyles } from "@emotion/react";
import { InputSize } from "../Input/Input";
export declare const radioButton: ({ typeScaleSizes, formElements: { radioButtonGroup } }: Theme, { size }: {
    size: InputSize;
}) => SerializedStyles;
