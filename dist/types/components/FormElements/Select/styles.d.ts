import { SerializedStyles, Theme } from "@emotion/react";
import { InputSize } from "../Input/Input";
export declare const selectContainer: ({ formElements }: Theme, { size }: {
    size: InputSize;
}) => SerializedStyles;
