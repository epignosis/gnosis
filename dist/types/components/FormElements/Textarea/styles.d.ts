import { SerializedStyles, Theme } from "@emotion/react";
import { TextareaResize } from "./Textarea";
export declare const textareaContainer: ({ formElements }: Theme, { resize }: {
    resize: TextareaResize;
}) => SerializedStyles;
