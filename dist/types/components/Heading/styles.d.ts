import { Theme, SerializedStyles } from "@emotion/react";
import { TypographyLevels } from "@theme/utils/typography";
export declare const headingContainer: ({ typeScaleSizes }: Theme, { size }: {
    size: TypographyLevels;
}) => SerializedStyles;
