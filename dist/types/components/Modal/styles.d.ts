import { Theme, SerializedStyles } from "@emotion/react";
import { Size } from "./Modal";
export declare const modalContent: SerializedStyles;
export declare const modalHeader: ({ modal }: Theme) => SerializedStyles;
export declare const modalFooter: ({ modal }: Theme) => SerializedStyles;
export declare const portalStyles: (size: Size) => SerializedStyles;
