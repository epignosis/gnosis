import { SerializedStyles, Theme } from "@emotion/react";
export declare const mainNavContainer: ({ sidebar }: Theme) => SerializedStyles;
export declare const navHandleContainer: ({ sidebar }: Theme) => SerializedStyles;
export declare const navItemContainer: ({ sidebar }: Theme) => SerializedStyles;
