import { Story } from "@storybook/react";
import { THEME_COLOURS as defaultTheme } from "./colors";
declare const _default: {
    title: string;
};
export default _default;
declare type StoryColors = {
    colors: typeof defaultTheme;
};
export declare const DefaultTheme: Story<StoryColors>;
